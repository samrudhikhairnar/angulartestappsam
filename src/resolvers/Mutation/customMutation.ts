
import { Context } from '../../utils'
import { Guid } from "guid-typescript";
export const customMutation = {
//   async createUser(parent, args, ctx: Context) {
//     args.data.TenantId       = Guid.create().toString();  
//     return ctx.db.mutation.createUser({ ...args}); 
//   },
}
